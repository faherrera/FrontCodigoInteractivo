import React from 'react';

const styles = {
    logoPrincipal:{
        fontWeight: "300",
    }
}
export const LogoPrincipal = () => {
    return <a href="/" style={styles.logoPrincipal}>Codigo Interactivo</a>
}